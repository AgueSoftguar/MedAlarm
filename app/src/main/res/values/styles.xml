<resources>

    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="AppTheme.Text" />

    <style name="AppTheme.Text.ToolBar" />

    <style name="AppTheme.Text.ToolBar.EditText">
        <item name="colorControlNormal">@android:color/white</item>
        <item name="colorControlActivated">@android:color/white</item>
    </style>

    <!-- Add and edit medicine screen -->
    <style name="AppTheme.Text.AddAndEditMedicine" />

    <style name="AppTheme.Text.AddAndEditMedicine.Text">
        <item name="android:textColor">@color/add_and_edit_medicine_text_view</item>
        <item name="android:textSize">@dimen/add_and_edit_medicine_text_size</item>
        <item name="android:textColorHint">@color/add_and_edit_medicine_edit_text_hint</item>
    </style>

</resources>
